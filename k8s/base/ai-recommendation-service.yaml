apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-recommendation-service
  namespace: amitra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-recommendation-service
  template:
    metadata:
      labels:
        app: ai-recommendation-service
    spec:
      containers:
      - name: ai-recommendation-service
        image: ${ARTIFACT_REGISTRY_REGION}-docker.pkg.dev/${GCP_PROJECT_ID}/${ARTIFACT_REPOSITORY}/ai-recommendation-service:${IMAGE_TAG}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: amitra-common-config
        - secretRef:
            name: amitra-common-secrets
        startupProbe:
          httpGet:
            path: /health
            port: 8000
          failureThreshold: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 15
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 20
        resources:
          requests:
            memory: 256Mi
            cpu: 100m
          limits:
            memory: 768Mi
            cpu: 1000m
---
apiVersion: v1
kind: Service
metadata:
  name: ai-recommendation-service
  namespace: amitra
spec:
  type: ClusterIP
  selector:
    app: ai-recommendation-service
  ports:
  - name: http
    port: 8000
    targetPort: 8000
